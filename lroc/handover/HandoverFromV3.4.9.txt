LROC Handover From v3.4.9
Timestamp: 2026-06-14 15:13:21 AEST

Build: Article Renderer No-Blank Refinement

Context
- v3.4.7 flowed article content across pages, but bullets disappeared again and whitespace was still not reduced enough.
- Tony supplied td5_gearbox_article_A4_2column_no_blank(1).pdf as the visual target for article body layout.
- The target is not to copy every decorative trim. Retain LROC's existing footer style. The article body should fill non-final pages like the supplied two-column A4 example.

What changed
- TinyMCE article list splitting now keeps a single valid class attribute and preserves list markers.
- Generated article pages now use explicit two-column page HTML fragments for placed pages instead of depending on browser multi-column balancing.
- Movable media blocks are checked against real column height and can be reduced before they create a large blank bottom area.
- A heading followed by a media block can shrink the media to fit instead of forcing an early page break.
- Renderer diagnostics report explicit column pages, media column adjustments, and heading/media shrink-to-fit counts.

Files touched
- lroc/VERSION
- lroc/site/app.js
- lroc/site/styles.css
- lroc/site/service-worker.js
- lroc/changelog/changelog_3.4.9.md
- lroc/handover/HandoverFromV3.4.9.txt

Testing notes
1. Deploy and clear/reload the PWA cache if needed.
2. Re-place the article fresh in Issue Builder so the generated page fragments are rebuilt by v3.4.9.
3. Confirm bullet markers appear in the Td5 lessons/checklist page.
4. Confirm non-final pages have no large avoidable white holes. The last article page may still have white space.
5. Confirm the existing LROC footer style is retained.

Known continuation
- This is still heuristic browser-side pagination. The longer-term destination remains a deterministic immutable page renderer/PDF pipeline.
