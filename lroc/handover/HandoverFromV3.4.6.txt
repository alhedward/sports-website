LROC Handover — v3.4.6 Article Renderer Viewport Stabilisation
Generated: 2026-06-14 AEST

Baseline
- v3.4.5 restored MCE article page payload rendering, but Issue Builder preview still showed a large blank/oval image area after proof/editor toggles.
- User reported a transient flicker where the real article appeared about 150% overfit to the viewport, then was replaced by the bad preview state.

What changed
- mag340FitMceArticlePages now clamps preview scale to <= 1.0. The article page should only scale down to fit the available slot, never enlarge.
- The MCE article window is absolutely positioned inside the generated page slot. The diagnostics note is also absolutely positioned, so it cannot steal flow height from the article renderer.
- CSS now supplies an initial scale before JS fitting runs, reducing the visible oversized first paint.
- Generic Issue Builder image thumbnail CSS is overridden inside .render-mce_article_page so article images do not become forced 100%-wide / 180px-tall previews.
- Renderer diagnostics now report 3.4.6 and pagination mode column_width_block_aware_v346.

Continue from here
- If the article still shows the large oval, inspect the saved slot payload in the browser console: layoutSlots[0].articlePageHtml should contain headings/paragraphs as well as images.
- If only an image is present in articlePageHtml, the paginator is returning a bad first fragment. Patch mag340MceTopLevelBlocks / mag340McePaginateHtmlByBlocks next.
- If articlePageHtml is correct but preview is wrong, continue tightening CSS isolation for .render-mce_article_page.

Validation expected
- python3 -m py_compile lroc/lambda/member_files.py lroc/lambda/magazine_api.py
- node --check lroc/site/app.js
- node --check lroc/site/service-worker.js
- node --check lroc/site/expo/expo.js
