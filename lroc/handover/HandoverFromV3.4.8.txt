LROC Handover From v3.4.8
==========================

Timestamp: 2026-06-14 14:32:55 AEST
Package: lroc-deployer-integration-v3.4.8

Current baseline
----------------
- v3.4.7 successfully placed TinyMCE article content across the selected issue pages.
- Next renderer target is whitespace reduction/column and image placement refinement.
- Non-final pages should avoid large blank regions; the final article page may have remaining white space.

This package
------------
- Adds the local deploy helper into the repo at:
  - lroc/deployer/deploy_helper.py
  - lroc/deployer/file.deploy.txt
  - lroc/deployer/README.md
- The deploy helper is the verbose GitHub Actions watcher version that can monitor job/step progress after git push.
- The package root lroc/file.deploy.txt now stages lroc/deployer during deploy so the helper files are committed to the repo.

Deployment notes
----------------
- The deploy helper still uses package-provided lroc/file.deploy.txt as the authoritative deploy script when deploying a package.
- The copy at lroc/deployer/file.deploy.txt is a reference/template bundled with the helper.
- GitHub Actions watch requires gh authentication on the local machine:
  gh auth login
  gh auth status

Next recommended work
---------------------
1. Continue article renderer whitespace elimination.
2. Improve image placement so image blocks can shrink or move without leaving holes in previous columns/pages.
3. Add preview diagnostics showing leftover page/column height and which block caused a break.
4. Preserve the rule that issue page count remains a multiple of 4 and locked pages/panels are hard constraints.
