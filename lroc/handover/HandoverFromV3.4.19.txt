LROC handover from v3.4.19 — A4 Preview Phantom Page Trim

Current focus:
- Article Library A4 layout preview and Issue Builder article renderer refinement.

What changed in v3.4.19:
- Added clipped-window page usage measurement for the TinyMCE article renderer.
- The renderer now builds hidden page windows, applies the same horizontal translateX offset used by the real preview, and counts only pages with actual visible text glyphs/media inside the clipped window.
- Article Library A4 preview and Issue Builder placement both use that final meaningful page count.
- This should remove the remaining phantom continuation page when the article meaningfully ends on page 2.
- Kept v3.4.13 image caption behaviour, v3.4.15 manual image move controls, and v3.4.18 preview-refresh button wiring.

Known context:
- User confirmed caption double-click works well and captions render correctly. Preserve this.
- User can manually move the offending image to get the desired layout. Preserve image earlier/later controls.
- Avoid the old explicit-column packer from v3.4.7/v3.4.9 because it damaged bullets and spacing.
- Refresh A4 preview is intentionally local/client-side, so no Network tab activity is expected.

Next checks:
- Confirm Article Library A4 preview shows only the two meaningful pages for the TD5 gearbox article after manual image placement.
- Confirm Issue Builder no longer assigns the phantom 3rd page.
- Confirm captions and font formatting remain intact.
