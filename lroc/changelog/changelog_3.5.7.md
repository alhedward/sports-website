# LROC v3.5.7 — Front Cover Undo and Full-Width Bars

- Added a front-cover layout revert stack so preset/format changes and control edits can be backed out through previous cover states.
- Changed front-cover top and bottom bars to full-width overlay bars with editable vertical position and up/down controls.
- Reworked the bottom bar into a centred full-width top row plus two centred lower cells.
- Added editable bottom-bar Facebook URL/right-cell text.
- Added separate bottom-bar top-row font size and shared second-row font size for the two lower cells.
- When both lower cells are blank, the bottom-bar top row is vertically centred in the bar.
- Kept cover save semantics unchanged; preview/revert changes are only persisted when Save cover layers is clicked.
