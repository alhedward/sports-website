# v3.5.5 — A4 Preview Browser Zoom Stabilisation

- Changed the Article Library A4 preview scaling from CSS `zoom` to a transform-scale wrapper.
- Fixes the 90% browser zoom case where an advert that otherwise fitted could disappear from the A4 preview.
- No intended change to TinyMCE editing, advert captions, Issue Builder placement, or region-placement foundation.
